# Para começar a usar o pandas na máquina o primeiro passo é instalar o pacote no computador com sintax abaixo:
# pip install pandas

# "import" é o comando usado para trazer o pacote para uso no seu arquivo python, você pode atribuir um apelido para a biblioteca incrementando o "as". 
# A comunidade costuma utilizar o apelido "pd", porém fica a critério do desenvolvedor escolher o apelido (alias).

import pandas as pd

print(pd.__version__) # Comando usado para verificar a versão do pacote;