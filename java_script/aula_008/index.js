// var x let

var nome = 'Emanoel';
var nome = 'Gabriel';

// Usar 'var' permite que declare uma variável que já foi declarada, o que foi ajustado na ES6 (ECMAScript 2015), adicionando a nova funcionalidade
// do 'let';

// Observação, é possível declarar uma variável sem usar nenhum tipo de função antecedendo o nome da variável;
// Entretanto, isto não é uma prática, pois acaba criando uma variável global que pode comprometer outra variável;

// USE SOMENTE 'let' variáveis e 'const' constante;
